@model IEnumerable<VuVanDuc2022483.Models.VVD0483>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.VVDName)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.VVDGender)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.VVDName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.VVDGender)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.VVDId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.VVDId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.VVDId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
